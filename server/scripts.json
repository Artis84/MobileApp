{
    "$schema": "https://deno.land/x/denon@2.5.0/schema.json",
    "scripts": {
        "crud-dev": {
            "cmd": "deno run --allow-net --allow-env index.js && set NODE_ENV=development",
            "desc": "run my development crud server"
        },
        "crud-prod": {
            "cmd": "deno run --allow-net --allow-env index.js",
            "desc": "run my production crud server"
        }
    }
}
